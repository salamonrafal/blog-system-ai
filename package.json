{
  "name": "blog-system-ai-assets",
  "private": true,
  "type": "module",
  "scripts": {
    "build:assets": "node scripts/build-assets.mjs",
    "build:assets:dev": "node scripts/build-assets.mjs dev",
    "build:assets:prod": "node scripts/build-assets.mjs prod",
    "build:prod": "npm run build:assets:prod",
    "test:js": "vitest run",
    "test:js:watch": "vitest"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "esbuild": "^0.27.7",
    "jsdom": "^29.0.2",
    "vitest": "^4.1.5"
  }
}
